{
    "private": true,
    "type": "module",
    "scripts": {
        "dev:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-team-management.css --watch",
        "dev:scripts": "node bin/build.js --dev",
        "build:styles": "npx @tailwindcss/cli -i resources/css/index.css -o resources/dist/filament-team-management.css --minify",
        "build:scripts": "node bin/build.js",
        "dev": "npm-run-all --parallel dev:*",
        "build": "npm-run-all build:*"
    },
    "devDependencies": {
        "@tailwindcss/cli": "^4.0",
        "esbuild": "^0.25.0",
        "npm-run-all": "^4.1.5",
        "prettier": "^3.3.0",
        "prettier-plugin-tailwindcss": "^0.6.0",
        "tailwindcss": "^4.0"
    }
}
